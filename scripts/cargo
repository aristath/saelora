#!/bin/sh
set -eu

if command -v cargo >/dev/null 2>&1; then
  exec cargo "$@"
fi

if [ -x "$HOME/.cargo/bin/cargo" ]; then
  exec "$HOME/.cargo/bin/cargo" "$@"
fi

echo "cargo not found. Install Rust (rustup) or add \$HOME/.cargo/bin to PATH." >&2
exit 127

